name: "Build"

on:
  push:
    tags:
      - '**'

jobs:
  build:
    uses: LerianStudio/github-actions-shared-workflows/.github/workflows/build.yml@v1.10.2
    with:
      runner_type: "blacksmith-4vcpu-ubuntu-2404"
      enable_dockerhub: true
      enable_ghcr: true
      dockerhub_org: lerianstudio
      enable_gitops_artifacts: true
    secrets: inherit

  update_gitops:
    needs: [build]
    if: needs.build.result == 'success'
    uses: LerianStudio/github-actions-shared-workflows/.github/workflows/gitops-update.yml@v1.10.2
    with:
      runner_type: "firmino-lxc-runners"
      artifact_pattern: "gitops-tags-tracer"
      gitops_file_dev: "gitops/environments/firmino/helmfile/applications/dev/tracer/values.yaml"
      gitops_file_stg: "gitops/environments/firmino/helmfile/applications/stg/tracer/values.yaml"
      gitops_file_prd: "gitops/environments/firmino/helmfile/applications/prd/tracer/values.yaml"
      gitops_file_sandbox: "gitops/environments/firmino/helmfile/applications/sandbox/tracer/values.yaml"
      yaml_key_mappings: '{"tracer.tag": ".tracer.image.tag"}'
    secrets: inherit
  